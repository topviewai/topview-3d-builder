# Scene3D Studio 只用本地配置，不需要任何账号或网络。复制为 .env.local 后按需填写。

# 内置素材库目录（含 manifest.json）。缺省为仓库根的 builtin-assets/。
# TOPVIEW3D_BUILTIN_ASSETS=

# 要只读打开的 CLI 项目目录，多个用系统路径分隔符隔开（macOS / Linux 为 ":"，Windows 为 ";"）。
# 这些项目的 .topview3d/assets/ 素材也会加入素材面板。
# TOPVIEW3D_PROJECTS=
